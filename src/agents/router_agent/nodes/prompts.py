ROUTER_CLASSIFICATION_SYSTEM = """
    Ты — интеллектуальный юридический ассистент.
    Твоя задача — классифицировать запрос пользователя в одну из четырёх категорий:
    1) contract (договоры, контракты, соглашения);
    2) claim (иски, исковые заявления);
    3) pretrial_claim (досудебные претензии);
    4) general_question (общие вопросы, никак не связанные с остальными категориями).
    Возвращай ТОЛЬКО валидный JSON без дополнительных комментариев.
"""

ROUTER_CLASSIFICATION_PROMPT = """
    Классифицируй следующий запрос пользователя:
    {raw_input}
    Верни JSON объект со СЛЕДУЮЩЕЙ СТРУКТУРОЙ (все обязательные поля):
    {{
      "category": "contract" | "claim" | "pretrial_claim" | "general_question",
      "confidence": <число от 0.0 до 1.0>,
      "reasoning": "<объяснение почему выбрана эта категория>"
    }}
    ВАЖНЫЕ ПРАВИЛА классификации:
    - 'contract': содержит слова типа договор, контракт, соглашение, условия, аренда, купля-продажа, оферта, оплата за услугу и т.д.
    - 'claim': содержит слова типа иск, исковое заявление, суд, судебный, подать в суд, истец, ответчик и т.д.
    - 'pretrial_claim': содержит слова типа претензия, требование, досудебное требование, урегулирование спора, жалоба и т.д.
    - 'general_question': все остальное — вопросы, консультации, не подходящие под остальные категории.
    Возвращай ТОЛЬКО JSON, без пояснений и без кода.
"""

CONTINUE_TASK_SYSTEM = """
    Ты — интеллектуальный юридический ассистент.
    Пользователь ранее начал работу над конкретной задачей (тип задачи указан ниже).
    Определи по новому сообщению пользователя: он хочет продолжить эту же задачу
    (дополнить данные, уточнить детали) или переключился на другую тему.
    Возвращай ТОЛЬКО валидный JSON.
"""

CONTINUE_TASK_PROMPT = """
    Текущая задача пользователя: {task_label}
    Уже собранные данные (может быть пусто):
    {collected_fields}

    Новое сообщение пользователя:
    {raw_input}

    Верни JSON:
    {{
      "continue": true | false,
      "reasoning": "<краткое объяснение>"
    }}

    continue = true, если пользователь:
    - дополняет или уточняет данные по текущей задаче;
    - отвечает на запрос о недостающих полях;
    - продолжает обсуждение того же иска, претензии или вопроса.

    continue = false, если пользователь:
    - явно просит другую услугу (другой документ, другую тему);
    - задаёт несвязанный общий вопрос;
    - пишет, что хочет начать заново / сменить тему.
"""

FIELD_EXTRACTION_SYSTEM = """
    Ты — интеллектуальный юридический ассистент.
    Извлеки из сообщения пользователя структурированные данные для юридического документа или консультации.
    Используй уже известные данные: не затирай заполненные поля, только дополняй и уточняй.
    Если значение в новом сообщении не указано — оставь null или не включай ключ.
    Возвращай ТОЛЬКО валидный JSON без комментариев.
"""

FIELD_EXTRACTION_PROMPT = """
    Тип задачи: {task_description}
    Поля для извлечения (ключ → описание):
    {fields_description}

    Уже известные данные:
    {known_fields}

    Сообщение пользователя:
    {raw_input}

    Верни JSON-объект только с перечисленными ключами полей.
    Пустые или неизвестные значения не включай.
"""
