server {
    listen 80;

    location /api/ {
        proxy_pass http://backend:8000/api/;
    }

    location / {
        alias /staticfiles/;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}